export class createBuyWindow {
    startDate ?: any;
    endDate ?: any;
    buyWindowName ?: string;
    buyWindowId ? :number;
    statusName? : string;
    createdDate? : Date;
    recentBuy ? : boolean;
}